apiVersion: v1
kind: Service
metadata:
  name: storefront
  namespace: storefront
spec:
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app.kubernetes.io/name: storefront
