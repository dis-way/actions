apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      app.kubernetes.io/name: storefront

resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
